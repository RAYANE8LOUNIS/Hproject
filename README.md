# Hproject

This is a repository of software group project in westminister university
Group members include:

how to run:
Clone the repo, go into Hproject-backend (cd Hproject-backend), then run:

python -m venv venv
venv\Scripts\activate
pip install django django-cors-headers openpyxl
python manage.py migrate
python manage.py runserver
